"use strict";

// TODO - write your code here.
//
const randomDamage = () =>{
    return(Math.floor((Math.random() * 10) + 1)
}

const chooseOption =(opt1, opt2) {
    let randNum = Math.floor(Math.random() * 11) => {
    return (randNum) 
    else return opt1:opt2;
}

function attackPlayer= (health-randomDamage)

function logHealth (player, health)
    console.log(`${player}+ logHealth + health`)
    return {health<=0)
}
function isDead(health)

function fight (player1,player2, player1Health, player2health)
    while(attacker==chooseOption(player1,))

const attacker = chooseOption(player1, player2);
    if(attacker == player1){player2Health=attackPlayer}

    }

}
